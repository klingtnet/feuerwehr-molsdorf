<header class="header">
	
	<nav class="navbar navbar-toggleable-md navbar-inverse">
		<div class="container">
			<button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		    <span class="navbar-toggler-icon"></span>
		  </button>

			<a class="navbar-brand" href="/">
				{{fa-icon "fire-extinguisher"}}
				{{t "application.brand.short"}}
				<div class="navbar-text">
					<small>{{t "application.brand.description"}}</small>
				</div>
			</a>

			<div class="collapse navbar-collapse justify-content-md-end" id="navbarNav">
		    <div class="navbar-nav">
					{{#each model.navigation as |item|}}
						{{#if item.active}}
		      		<a class="nav-item nav-link active" href="{{item.link}}">{{t item.localization}}</a>
						{{else}}
							<a class="nav-item nav-link" href="{{item.link}}">{{t item.localization}}</a>
						{{/if}}
					{{/each}}
		    </div>
		  </div>
		</div>
	</nav>

</header>

<div class="application-content">
	{{outlet}}
</div>

<footer class="footer">
	<div class="container">
		xxx
	</div>
</footer>
